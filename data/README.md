# 数据目录

`foods.csv` 是便于审阅和后续导入的 V1 数据快照，空单元格代表未知值，不代表零。

当前网页运行时使用 `src/data/foods.js`，两处数据应在同一个 Pull Request 中同步修改。后续数据量扩大后，应增加自动生成脚本，避免长期维护两份人工副本。

字段说明见 [`docs/data-methodology.md`](../docs/data-methodology.md)，已知缺口见 [`docs/data-gaps.md`](../docs/data-gaps.md)。
